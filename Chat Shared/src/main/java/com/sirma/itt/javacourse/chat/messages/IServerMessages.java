package com.sirma.itt.javacourse.chat.messages;

/**
 * An enum containing all the messages that can be sent from the server to the client.
 * 
 * @author user
 */
public enum IServerMessages {
	INVALID_NICK, NICK_OK, MESSAGE, CLIENT_DISCONNECTED, ADD_TO_LIST, USER_LIST;
}
